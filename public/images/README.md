Place your company logo at `public/images/logo.png` so the site displays it in the header and footer.

Recommended specs:
- Format: PNG or WebP
- Size: around 1200x1200 (will be resized client-side)
- Transparent background recommended

After adding the file restart the dev server if necessary:

```bash
npm run dev
```
